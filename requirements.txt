# === 月白 AI Agent — Railway 轻量版依赖 ===
# Python 3.10+ required

# Web 框架
flask>=3.0
flask-cors>=4.0
gunicorn>=22.0

# LLM（必需）
openai>=1.50.0
httpx>=0.27.0

# 注：不依赖 sentence-transformers / chromadb / FlagEmbedding / rank-bm25 / crewai 等重型库
# 这样 Railway 部署秒级启动，避免健康检查超时
